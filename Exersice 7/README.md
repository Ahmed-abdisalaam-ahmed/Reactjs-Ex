### **Challenge 2: Mouse Position Tracker**

1. Create a component called **`MouseTracker`**.
2. Use **state** to store the current mouse coordinates: `{ x, y }`.
3. In `useEffect`, **add** a `mousemove` event listener that updates these coordinates.
4. Cleanup the event listener when unmounting.